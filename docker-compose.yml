version: '3.8'
services:
  immiaccount:
    build: .
    ports:
      - "8080:5000"
    volumes:
      - immiaccount_data:/data
    environment:
      - FLASK_ENV=production
volumes:
  immiaccount_data:
